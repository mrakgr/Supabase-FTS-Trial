{
  "scripts": {
    "base": "tsc && esbuild frontend/*.ts --bundle --outdir=static/bundles/",
    "build": "npm run base -- --sourcemap",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "lit": "^3.1.3"
  }
}
